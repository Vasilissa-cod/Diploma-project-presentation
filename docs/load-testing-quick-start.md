# БЫСТРЫЙ СТАРТ: Нагрузочное тестирование FatSecret

## 🚀 Быстрая настройка JMeter (5 минут)

### 1. Создание базового теста

1. **Запустите JMeter**
2. **File → New** (создать новый тест-план)
3. **Правой кнопкой на Test Plan → Add → Threads (Users) → Thread Group**

### 2. Настройка Thread Group для Теста 1 (10 пользователей)

```
Name: Load Test - 10 Users
Number of Threads: 10
Ramp-up Period: 10 секунд
Duration: 60 секунд (или Loop Count: 6)
```

### 3. Добавление HTTP Request

**Правой кнопкой на Thread Group → Add → Config Element → HTTP Request Defaults**
```
Server Name: foods.fatsecret.com
Protocol: https
```

**Правой кнопкой на Thread Group → Add → Sampler → HTTP Request**
```
Name: Main Page
Method: GET
Path: /Default.aspx
```

### 4. Добавление Listeners

**Правой кнопкой на Thread Group → Add → Listener → Summary Report**
**Правой кнопкой на Thread Group → Add → Listener → Aggregate Report**

### 5. Запуск теста

1. Выберите Thread Group
2. Нажмите **▶ Start** (зеленая кнопка)
3. Дождитесь завершения
4. Посмотрите результаты в Summary Report

---

## 📊 Ключевые метрики для анализа

| Метрика | Ожидаемое значение | Где смотреть |
|---------|-------------------|--------------|
| **Average (среднее время)** | < 2000 мс | Summary Report / Aggregate Report |
| **Error %** | 0% | Summary Report |
| **Throughput** | > 5 req/sec | Summary Report |
| **Min/Max** | Min < 500 мс, Max < 5000 мс | Aggregate Report |

---

## ⚙️ Настройки для Теста 2 (50 пользователей)

```
Name: Stress Test - 50 Users
Number of Threads: 50
Ramp-up Period: 60 секунд
Duration: 300 секунд (5 минут)
```

---

## 💾 Сохранение результатов

1. **Summary Report → Правой кнопкой → Save Table Data**
2. Сохраните как CSV: `results_10_users.csv`
3. Сделайте скриншот Summary Report

---

## ⚠️ Важные замечания

- Отключите **View Results Tree** для больших тестов (потребляет много памяти)
- Сохраняйте тест-план перед запуском: **File → Save**
- Для теста 50 пользователей используйте не-GUI режим (командная строка)

---

## 🔧 Команда для не-GUI режима

```bash
jmeter -n -t FatSecret_Load_Test.jmx -l results.jtl -e -o report/
```

---

*Для подробной инструкции см. load-testing-guide.md*

